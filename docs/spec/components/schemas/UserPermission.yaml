allOf:
  - $ref: "#/components/schemas/UserPermissionKey"
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - link
          - path
          - access_level
        properties:
          username:
            type: string
            description: username from telegram
            example: "velykodnyi"
          user_id:
            type: integer
            format: int64
            description: user id from identity
            example: 666
          link:
            type: string
            description: chat title
            example: "WE vs. ACS"
          path:
            type: string
            description: workspace title
            example: "DL"
          access_level:
            type: object
            description: level of success for workspace
            $ref: "#/components/schemas/AccessLevel"

